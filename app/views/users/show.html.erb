<p style="color: green"><%= notice %></p>

<p>
<strong>Username:</strong>
<%= @user.username %>
</p>

<% if @user.ratings.empty? %>
  <p>User doesn't have any ratings!</p>
<% else %>
  <p>
  Favorite beer:
  <%= @user.favorite_beer %>
  </p>

  <p>
  Favorite style:
  <%= @user.favorite_style %>
  </p>

  <p>
  Favorite brewery:
  <%= @user.favorite_brewery %>
  </p>
  
  <p>User has done <%=pluralize(@user.ratings.count, 'rating')%> with average of <%= @user.average_rating %></p>
<% end %>

<ul>
  <% @user.ratings.each do |rating| %>
    <li>
      <%= "#{rating.beer.name} #{rating.score}" %>
      <% if @user == current_user %>
        <%= button_to 'Delete', rating, method: :delete, form: { style: 'display:inline-block;',  data: { 'turbo-confirm': 'Are you sure?' } } %>
      <% end %>
    </li>
  <% end %>
</ul>

<strong>Joined clubs:</strong>

<ul>
  <% @user.clubs.each do |club| %>
    <li> <%= link_to club.name, club %> </li>
  <% end %>
</ul>

<div>
  <% if @user == current_user %>
    <%= link_to "Edit this user", edit_user_path(@user) %> | 
  <% end %>
  <%= link_to "Back to users", users_path %>
  <% if @user == current_user %>
    <%= button_to "Destroy this user", @user, method: :delete %>
  <% end %>
</div>
